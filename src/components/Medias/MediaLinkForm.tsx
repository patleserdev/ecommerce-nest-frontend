type Props = {
    entity : string,
    id : number
}

export default function MediaLinkForm({entity,id}:Props){

// s'il y a deja des links pour cette entité je les affiche avec leur role  et je les positionne

// sinon j'ouvre la médiathèque dans le même modal ( slidable = cool)
    return(
        <div>
            
        </div>

    )
}